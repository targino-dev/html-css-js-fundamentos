// Variável se o boleto está pago
let boletoPago = false;
// Condicional se o boleto está pago - Estrutura If/Else
if (boletoPago == true) {
    alert("O boleto está pago!");
} else {
    alert("O boleto não está pago!");
}
